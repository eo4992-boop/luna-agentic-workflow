# Luna v2 — 프로젝트 초기화 규칙

이 문서는 새 소프트웨어 프로젝트를 Luna v2로 시작할 때 ChatGPT가 따라야 하는 초기화 규칙이다.

## 표준 호출

사용자는 새 ChatGPT 대화에서 다음 한 줄을 입력할 수 있다.

> **Luna v2로 이 저장소를 시작해줘: `<GitHub repository URL>`**

이 한 줄은 Luna v2 전체 워크플로를 매번 설명하는 대신, 재사용 가능한 방법론을 호출하는 표준 진입점이다.

## 초기화 프로토콜

### 1. Workflow 확인

- `luna-agentic-workflow/WORKFLOW.md`를 읽는다.
- `START.md`와 이 문서를 참고한다.
- Workflow의 규범과 프로젝트 자체 규칙이 충돌하면 먼저 충돌을 식별하고 사용자에게 확인한다.

### 2. Project Context 수집

대상 저장소에서 먼저 확인한다.

- 기본 브랜치와 현재 HEAD
- README 및 프로젝트 문서
- `AGENTS.md` 등 저장소 규칙
- 기존 skills / automation / CI 설정
- 패키지 및 빌드 구조
- 테스트와 검증 방법
- 열린 작업, PR, 현재 변경 상태
- 알려진 제약과 위험

확인한 사실과 추정/미확인 사항을 구분한다.

### 3. Context → Intent → Spec → Plan

초기화 직후 바로 코딩하지 않는다.

1. **Context** — 현재 프로젝트를 이해한다.
2. **Intent** — 사용자가 원하는 최종 결과를 확정한다.
3. **Spec** — 완료 조건을 테스트 가능한 체크리스트로 만든다.
4. **Plan** — 작업을 검증 가능한 단위로 분해한다.

사용자에게는 결과를 실질적으로 바꿀 수 있는 질문만 한다.

### 4. 구현 시작 조건

다음이 충족되면 구현을 시작한다.

- 목표가 명확하다.
- 핵심 제약이 확인됐다.
- 완료 조건이 관찰 가능하다.
- 구현 범위가 합의됐다.
- 필요한 사용자 결정이 처리됐다.

### 5. 검증

구현 후 반드시:

- **5-A Gate:** 테스트, 빌드, 타입, 린트, 패키징, 스키마, diff 등의 결정론적 검증을 프로젝트에 맞게 수행한다.
- **5-B Independent Review:** 구현 과정과 독립된 관점에서 결과가 Spec을 만족하는지 판단한다.

5-A 실패는 구현으로 돌아간다.
5-B 실패는 Spec으로 돌아간다.

### 6. 완료 조건

다음만 완료로 선언한다.

- 실제로 확인한 검증 결과가 있다.
- Spec의 완료 조건을 충족한다.
- 미검증 항목과 제한사항이 명시돼 있다.
- 필요한 경우 GitHub에 durable handoff가 남아 있다.

## 하지 않는 것

- 원본 `agentic-workflow-playbook`을 매 프로젝트마다 재분석하지 않는다.
- Claude Code 전용 명령, 세션 구조, `~/.claude` 경로 등을 기본 절차로 사용하지 않는다.
- 단순히 "Luna v2"라고 했다는 이유만으로 기존 프로젝트 규칙을 덮어쓰지 않는다.
- 실행하지 않은 테스트를 통과했다고 주장하지 않는다.
- 5-A 통과만으로 최종 정확성을 선언하지 않는다.

## 프로젝트별 Context

Luna v2는 모든 프로젝트에 동일한 디렉터리 구조를 강제하지 않는다. 프로젝트에 지속적인 Context가 필요하면 해당 저장소의 기존 관례에 맞춰 `LUNA.md`, `.luna/`, 문서 파일 등 적절한 형태로 기록한다.
